apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "super-chart.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default "game" }}
  labels:
    {{- include "super-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "super-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
